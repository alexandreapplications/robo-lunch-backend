var CartController = require("./cartController");

describe("Card controller", () => {
  test("create method should call service", () => {
    const cartController = CartController(null);

    //cart
  });
});
